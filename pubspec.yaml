name: appturismo
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  appwrite: ^17.0.0
  get: ^4.6.5
  geolocator: ^10.1.0
  shared_preferences: ^2.5.3
  image_picker: ^1.1.0
  google_maps_flutter: ^2.5.0 # ✅ Paquete correcto para mapas

  flutter_dotenv: ^5.2.1
  http: ^1.4.0
  get_storage: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true


dependency_overrides:
  flutter_web_auth_2: ^4.1.0
  flutter_web_auth_2_platform_interface: ^4.1.0
